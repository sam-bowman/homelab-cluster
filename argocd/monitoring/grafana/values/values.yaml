datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: loki
      type: loki
      url: http://loki-gateway.loki:80
      jsonData:
        derivedFields:
          - datasourceUid: Tempo
            matcherRegex: '"[tT]race(?:.?)[iI][dD]":\s?"(.\w+)"'
            name: TraceID
            url: "$${__value.raw}"
    - name: tempo
      type: tempo
      url: http://tempo.tempo:3100
      access: proxy
      basicAuth: false
    - name: victoria-metrics
      type: prometheus
      url: "http://vmsingle-victoria-metrics-victoria-metrics-k8s-stack.victoria-metrics:8429"
      access: proxy
admin:
  existingSecret: "grafana-admin-details"
  userKey: admin-user
  passwordKey: admin-password
dashboardProviders:
  dashboardproviders.yaml:
    apiVersion: 1
    providers:
    - name: 'default'
      orgId: 1
      folder: 'default'
      type: file
      disableDeletion: true
      editable: true
      options:
        path: /var/lib/grafana/dashboards/standard
dashboards:
  default:
    kubernetes-cluster-ram-and-cpu-utilization:
      gnetId: 16734
      datasource: victoria-metrics