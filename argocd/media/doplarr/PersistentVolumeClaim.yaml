apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: doplarr-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
  volumeMode: Filesystem