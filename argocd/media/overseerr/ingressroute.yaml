apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: overseerr-ingress-route
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`overseerr.homelab.bowmans.site`)
      kind: Rule
      services:
      - name: overseerr-k8s
        port: 5055
      middlewares:
        - name: kube-system-private-ips-allow@kubernetescrd
  tls:
    secretName: overseerr-certificate