apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: overseerr-certificate
spec:
  secretName: overseerr-certificate
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer-letsencrypt
  commonName: overseerr.homelab.bowmans.site
  dnsNames:
    - overseerr.homelab.bowmans.site