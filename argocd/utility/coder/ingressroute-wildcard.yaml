apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: coder-wildcard-ingress-route
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`5000--main--red-mosquito-46--smam-admin.coder.homelab.bowmans.site`)
      kind: Rule
      services:
      - name: coder
        port: 80
  tls:
    secretName: coder-certificate