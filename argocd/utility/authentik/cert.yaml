apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik-certificate
spec:
  secretName: authentik-certificate
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer-letsencrypt
  commonName: authentik.bowmans.site
  dnsNames:
    - authentik.bowmans.site