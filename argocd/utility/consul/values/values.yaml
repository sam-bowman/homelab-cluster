client:
  enabled: true
  resources:
    requests:
      memory: "400Mi"
      cpu: "200m"
    limits:
      memory: "400Mi"
      cpu: "200m"

connectInject:
  apiGateway:
    manageExternalCRDs: false
