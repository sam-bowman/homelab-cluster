apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: adguard-certificate
spec:
  secretName: adguard-certificate
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer-letsencrypt
  commonName: adguard.bowmans.site
  dnsNames:
    - adguard.bowmans.site